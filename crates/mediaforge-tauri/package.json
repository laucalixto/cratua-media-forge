{
  "name": "mediaforge-tauri",
  "private": true,
  "version": "0.2.2",
  "scripts": {
    "dev": "esbuild ui/src/main.js --bundle --outfile=ui/bundle.js --servedir=ui --serve=localhost:1420",
    "build": "esbuild ui/src/main.js --bundle --minify --outfile=ui/bundle.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "esbuild": "^0.25.0",
    "tailwindcss": "^4.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-dialog": "^2.7.1"
  }
}
